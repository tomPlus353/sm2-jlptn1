Create a diagram of a web system with an sqllite3 db. For the CLI side it has a python orator ORM and python CLI. In addition it should have a prisma/typescript/express as a web api and a frontend that uses next.js. The web front and back should be joined with graphql. In addition the express api should have be able to serve a line chatbot. Finally there should be a progressive web app for android phones. The entire system shares the same database and is a japanese vocabulary quiz app.
